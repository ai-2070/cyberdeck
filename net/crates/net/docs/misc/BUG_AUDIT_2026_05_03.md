# Bug audit — 2026-05-03

Multi-agent bug hunt across `net/crates/net/` (~117k LOC, 158 Rust files). Eight parallel agents each took a subsystem and reported real, demonstrable defects (not style or speculation). Findings sorted by severity.

## Critical

1. **`adapter/net/redex/disk.rs:1322` — `compact_to` is non-atomic on Windows.** `fsync_dir` is a no-op on `cfg(not(unix))`. Combined with the cross-file rename sequence (idx + dat + ts), a power loss can leave the directory pointing at mismatched inodes. Fix: use `MoveFileExW` with `MOVEFILE_WRITE_THROUGH`.

2. **`bus.rs:686-711` — Shutdown await order deadlocks.** `remove_shard_internal` awaits `workers.batch.await` before `workers.drain.await`, but the comment at lines 514-520 says drain must finish first so the batch worker's `recv()` can return `None`. Drain still owns a sender clone. Fix: swap the await order to drain → batch.

3. **`ffi/mod.rs:1322-1331` (`net_poll_ex`) — Panic across FFI on OOM.** `event.id.as_bytes().to_vec().into_boxed_slice()` and `event.raw.to_vec().into_boxed_slice()` can panic mid-loop, leaking earlier `Box::into_raw`s + the `std::alloc::alloc(layout)` array. No `catch_unwind`. Fix: wrap entry points in `catch_unwind`, or use `try_reserve`.

## High

4. **`adapter/net/redex/disk.rs:1356-1413` — Temp-file leak on `compact_to` failure.** Cleanup at 1411-1413 runs *after* `open_or_poison(...)?`/`clone_or_poison(...)?` (1356-1358, 1396-1398). On any post-rename reopen failure the three placeholder files in `/tmp` leak forever.

5. **`adapter/net/cortex/adapter.rs:486-499` — Watermark advances past unfolded events under `Stop` policy.** `recoverable_decode` causes `folded_through_seq.store(seq)` for events whose state mutation never landed; `wait_for_seq(seq)` then returns true incorrectly. Combined with snapshot-restore (#6) this produces permanent state divergence.

6. **`adapter/net/cortex/adapter.rs:374-386` + `tasks/adapter.rs:340-344` — Snapshot persists `last_seq` for skipped events.** When (5) fires, `snapshot()` returns `last_seq = Some(N)` for events whose state was never applied; restore tails from `N+1` and the skipped seq is never re-attempted. Violates "log is the source of truth."

7. **`adapter/net/cortex/meta.rs:135-137` — Checksum doesn't cover meta header.** `compute_checksum` hashes only `tail`. A bit-flip in `dispatch` byte (e.g. `STORED` → `DELETED`) is undetected and silently re-routes the event to the wrong fold arm.

8. **`adapter/net/state/log.rs:175-211` (`prune_through`) — `base_link` not updated on partial prune.** Anchor only refreshes when `events.is_empty()` post-prune. After a partial prune followed by a full prune, `base_link` points at the first prune anchor, not the current head — chain validation breaks.

9. **`adapter/net/redex/file.rs:1100-1126` (`sweep_retention`) — In-memory index corrupted on panic between drain and renormalize.** Lines 1100-1102 evict prefix; 1118-1126 rewrite `payload_offset`. A panic between them leaves rebased `base_offset` with absolute offsets — every subsequent read misses. Fix: build the renormalized index in a temp `Vec`, then atomically replace.

10. **`adapter/net/redex/disk.rs:251-261, 305-311` — Truncate-on-recovery without `sync_all`.** `set_len` for torn-tail repair has no `sync_all`/`fsync_dir`; a crash before the next durable write reverts the recovery and re-reads the same torn bytes.

11. **`adapter/net/redex/disk.rs:735, 951` — Best-effort rollback silently swallows errors.** `if let Ok(f) = OpenOptions::new().write(true).open(...)` — if the rollback open fails, orphan dat bytes / extra idx records persist. Subsequent appends produce permanent dat/idx divergence.

12. **`adapter/net/compute/registry.rs:71-74` (`replace`) — Not safe for stateful in-flight work.** A caller holding an old `Arc<Mutex<DaemonHost>>` from `get_arc` keeps mutating the old host while new traffic hits the new one; old chain's writes are dropped. `replica_group::on_node_failure`, `fork_group::on_node_failure`, and `standby_group::on_node_failure` all use this pattern without quiescing.

13. **`adapter/net/compute/orchestrator.rs:1301-1323` (`abort_migration_with_reason`) — Doesn't propagate to `MigrationSourceHandler`.** Source-side `migrations` map retains the entry; `is_migrating()` stays true, `buffer_event` keeps buffering into an undrained vector, retries trip `AlreadyMigrating`.

14. **`adapter/net/compute/standby_group.rs:354-389` — Replaced standby marked healthy with `synced_through = 0`.** If the active fails before next `sync_standbys`, `promote()` (`max_by_key(synced_through)`) may pick the fresh zero-state standby and lose all pre-buffer state. Fix: keep replaced standby unhealthy until after a successful sync, or filter promote candidates to `last_sync.is_some()`.

15. **`adapter/net/compute/bindings.rs:117-118` — Unbounded `Vec::with_capacity` from attacker u32.** `count = cursor.get_u32_le() as usize; Vec::with_capacity(count)` — peer-supplied snapshot can declare ~4B entries, ~96 GiB allocation before truncation checks. Fix: bound by `data.len() / MIN_BINDING_SIZE`.

16. **`adapter/net/cortex/tasks/watch.rs:64-86`, `memories/watch.rs:95-117` — Doc says strict bounds, code is inclusive.** `created_after` / `created_before` etc. published as `>` / `<`; underlying `…FilterSpec::matches` uses `>=` / `<=`. Strict-bound consumers receive boundary events.

17. **`adapter/net/reliability.rs:308-323` (`on_send`) — Silent reliability drop.** When `pending.len() >= max_pending`, the oldest *unacknowledged* packet is `pop_front`-ed; subsequent NACK can never recover that seq because the entry is gone (`on_nack` at 408-417 finds nothing to match). Fix: backpressure callers; don't drop tracking for in-flight packets.

18. **`adapter/net/router.rs:559-580` (`NetRouter::start`) — No re-entry guard.** Calling twice spawns two competing dequeue loops on the same `FairScheduler`, racing `pop()`. Fix: `compare_exchange` on `running`.

19. **`adapter/net/behavior/safety.rs:1378-1407` — AuditOnly mode silently drops violation logs.** `check_rate_limits` only logs the global-RPM violation when `mode == Enforce`; AuditOnly's documented "log violations but don't block" doesn't actually log. Fix: log unconditionally; gate only the `return Err` on enforce.

20. **`adapter/net/behavior/proximity.rs:316` (`update_from_pingwave`) — Worse path overwrites better.** Updates whenever `pw.seq > self.last_seq` regardless of hop count. A flooded high-seq pingwave through a long route demotes the cached direct route. Fix: separate freshness (always take latest seq) from path quality (only update `hops`/`addr`/`latency_us` when `new_hops <= self.hops`).

21. **`adapter/net/subnet/gateway.rs:144-152` — `ParentVisible` over-permissive direction.** Predicate accepts both `dest.is_ancestor_of(source)` and `source.is_ancestor_of(dest)`; the second clause leaks parent-region traffic *down* into descendants, contradicting the documented "visible to ancestors" framing. Tests cover only child→parent and sibling→sibling.

## Medium

22. **`adapter/net/cortex/adapter.rs:122-141` (`wait_for_seq`)** — When `start_seq == 0` and no events fold, the `u64::MAX` sentinel + always-true `running` makes `wait_for_seq(0)` block forever. Direct `CortexAdapter` users on an empty file hang.

23. **`adapter/net/cortex/memories/fold.rs:67-86` — Asymmetric upsert semantics under `STORED`.** Existing-id store partial-updates (preserves `pinned`/`created_ns`, overwrites the rest); missing-id inserts with `pinned: false`. The `MemoriesAdapter::store` doc reads "Store a new memory" — silent overwrite is surprising. Combined with absence of tombstones (`memories/fold.rs:115`), a STORED→DELETED→STORED sequence resurrects deleted entries.

24. **`adapter/net/cortex/memories/query.rs:77-86` — Empty-vec filter is a footgun.** `Some(vec![])` for `require_any_tag` excludes everything (`any` over empty = false → reject); `Some(vec![])` for `require_all_tags` excludes nothing (`all` over empty = true). UI forms emitting empty multi-selects break silently.

25. **`adapter/net/failure.rs:783-805` (`RecoveryManager::on_failure`)** — `failed_nodes.insert(...)` overwrites existing `FailedNodeState`, resetting `failed_at` and `retry_count` to 0. Flapping peers never hit `max_retries`. Fix: `entry().or_insert(...)` and bump `retry_count`.

26. **`adapter/net/session.rs:543-565` (`evict_idle_streams`) — LRU vs concurrent open race.** `streams.iter().min_by_key(...)` then `remove` is non-atomic. A freshly-opened stream can be torn down between selection and removal. Fix: `remove_if` with a freshness predicate.

27. **`adapter/net/mod.rs:451` — Handshake retry has no upper bound.** `tokio::time::sleep(Duration::from_millis(100 * attempt as u64))` over `MAX_HANDSHAKE_RETRIES = 1024` ⇒ ~14 hours total, last attempt sleeps ~102s. Fix: cap the per-attempt sleep at e.g. 5s.

28. **`adapter/net/redex/file.rs:402-462` (`append`) — Observer inconsistency.** `next_seq` is bumped before `segment.append`, so a reader between (`len()`, `next_seq()`) sees `len + 1 > next_seq_seen`. Fix: maintain a separate `committed_seq` for observers.

29. **`bus.rs:1337` — `JoinHandle` errors silently dropped.** `let _ = futures::future::join_all(drains).await;` — a panicked drain worker isn't logged unless the runtime's panic hook is configured (default Tokio doesn't). Stranded events.

30. **`adapter/net/identity/origin.rs:42-43` — `origin_hash: u32` collision floor.** ~65k peer birthday-collision; cross-channel accounting keyed by `origin_hash` aliases distinct entities. Fix: rename to `origin_tag` to dispel identity reading, or widen to u64 in a wire bump.

31. **`shard/mapper.rs:1024` (`finalize_draining`) — Wrong atomic ordering.** Reads `pushes_since_drain_start` with `Relaxed` while the field's docstring requires `Acquire` to pair with the writer's `SeqCst` reset.

32. **`adapter/net/identity/token.rs:565-594` (`insert_unchecked`) — Slot-cap race unbounded.** `contains_key` then `entry()` overshoot is bounded by concurrent calls, not shards. Fix: `entry().or_insert_with()` then drop on overflow.

33. **`adapter/net/behavior/diff.rs:241-243` — `to_bytes` returns `Vec::new()` on cap-violation.** Indistinguishable from a legitimate empty diff. Senders silently transmit zero bytes; receiver drops. Fix: deprecate `to_bytes` in favor of `try_to_bytes`.

34. **`adapter/net/contested/propagation.rs:62-106` — Lossy `f64 → u64` poisons EWMA.** A pathological RTT clamps `per_hop` to `u64::MAX`, locking `base_hop_latency_nanos` near max permanently. Combined with the `multiplier == 0.0` check (line 96) which doesn't catch NaN.

35. **`adapter/net/contested/correlation.rs:148` — `Instant` subtraction can panic.** `now - self.config.correlation_window` panics if `correlation_window > now.elapsed()` (configurable to hours).

36. **`adapter/net/contested/superposition.rs:107-111` — `target_replayed` only advances from `Superposed`.** If called while in `Spreading` (target catches up before `advance(Replay)`), phase stalls in `Spreading` forever; `ReadyToCollapse` never fires.

37. **`adapter/net/contested/observation.rs:62-73, 115` — Unbounded `HashMap` growth + `seq_diff_sum` overflow.** No eviction in `last_observed`/`estimated_delay`; `u64::abs_diff` accumulator overflows on long chains. Fix: LRU + `saturating_add`.

38. **`adapter/net/compute/orchestrator.rs:1177-1182` (`on_replay_complete`) — Forged `target_head` with `parent_hash: 0`.** Synthetic link doesn't match any real hash; downstream verifiers can't reconcile.

39. **`adapter/net/compute/migration_target.rs:244-261` (`buffer_event`) — No phase guard.** Inserts/delivers post-cutover; combined with normal-path delivery yields duplicate execution.

40. **`adapter/net/compute/migration_source.rs:107-145` (`start_snapshot`) — Snapshot side-effect race.** `contains_key` → `entry()` window allows two concurrent snapshots of the same origin via user `MeshDaemon::snapshot()`. Trait API doesn't enforce idempotency.

41. **`adapter/net/compute/standby_group.rs:213-253` (`sync_standbys`)** — Partial-failure leaves standbys at mixed states with `synced_through` unchanged and buffer not cleared; subsequent `promote()` may double-deliver.

42. **`adapter/net/compute/registry.rs:91-96` (`unregister`)** — In-flight `Arc<Mutex<DaemonHost>>` clones survive past unregister; a fast `register` of the same origin produces two parallel Arcs locking independently, splitting writes.

43. **`adapter/net/state/causal.rs:248` — Doc says `CausalLink: 24 bytes`, constant is 28.** Wire-format spec contradicts code; external parsers reading per the doc go off-by-4.

44. **`adapter/net/contested/partition.rs:241-246` — `NaN >= threshold` blocks healing.** When `other_side.is_empty()`, ratio is NaN and partition can't heal automatically.

45. **`adapter/net/redex/disk.rs:1196` — `saturating_sub(dat_base) as u32` for stale heap entry silently writes offset 0.** Currently safe by `dat_base` derivation, but masks corruption rather than failing.

46. **`adapter/net/traversal/portmap/natpmp.rs:537-606` (`remove`)** — Silent early returns on `bind`/`connect`/`send` failures; operator never sees that the prior mapping wasn't revoked.

47. **`consumer/merge.rs:384` — Per-shard cap clamps cursor advancement.** When `unclamped_per_shard > PER_SHARD_FETCH_CAP`, cursor rolls back instead of advancing to the last fetched event id; single-shard requests with filters can stall in re-fetch loops.

48. **`bus.rs:1245` — `std::time::Instant` mixed with tokio time.** Wall-clock `5s` in shutdown breaks `tokio::time::pause()`-based tests.

49. **`adapter/net/route.rs:461-481` — Doc/code mismatch on tie-break.** Doc says "preserved if strictly better"; code uses `<=`, allowing equal-metric overwrite.

50. **`ffi/mod.rs:563` (and similar handle accessors) — `&*handle` without alignment check.** Misaligned `*mut NetHandle` from C is immediate UB before the null check; only `is_null()` is validated.

## Lower-priority

- **`bus.rs:1064`** — sync `parking_lot::Mutex::lock` inside `async fn flush`.
- **`adapter/jetstream.rs:604, 628`** — unchecked `current_seq += 1` overflow on long-lived streams.
- **`shard/batch.rs:60`** — `total_events` unchecked add interacts with `saturating_sub` velocity → 0 across wraparound boundary.
- **`adapter/net/behavior/loadbalance.rs:984-985`** — NaN slips past `total_weight <= 0.0` (use `!(total_weight > 0.0)`).
- **`adapter/net/behavior/safety.rs:1259-1283`** — `release` uses `Relaxed` while `acquire` uses `AcqRel` — observable counter drift on weakly-ordered cores (ARM/RISC-V).
- **`adapter/net/behavior/safety.rs:1200-1206`** — audit-only token counter `fetch_add` without saturating.
- **`adapter/net/behavior/rules.rs:1247-1253`** — window reset to `1` not `0` admits `max-1` further firings.
- **`adapter/net/proxy.rs:317-322`** — telemetry counters incremented before send succeeds.
- **`adapter/net/failure.rs:818-841`** — `get_action` returns `Retry { delay_ms: 0 }` for healthy nodes, busy-loop footgun.
- **`adapter/net/behavior/loadbalance.rs:1183-1187`** — `select_power_of_two` degenerates to deterministic pair when `len == 2`.
- **`adapter/net/channel/roster.rs:80-91`** — TOCTOU between `is_empty()` and `remove_if`; idempotent today but fragile.
- **`adapter/net/channel/guard.rs:300-302`** — `revoke()` clears verified cache but bloom stays dirty until manual `rebuild_bloom`; false-positive rate climbs.
- **`adapter/net/identity/token.rs:411-422`** — `signed_payload()` allocates 95 bytes on every verify; hot-path waste.
- **`adapter/net/behavior/safety.rs:451-466`** — `per_source.clear()` at minute boundary drops just-incremented counters; brief per-source RPM cap exceedance.
- **`adapter/net/mod.rs:495-522`** — handshake recv loop allocates `BytesMut::with_capacity(MAX_PACKET_SIZE)` per iteration; allocator pressure under stray traffic.
- **`adapter/net/mod.rs:495-522`** — initiator handshake path lacks `HandshakePacer`-style rate limit.
- **`adapter/net/router.rs:198-222`** — `notify_one` lost-wakeup window bounded to 1ms by polling fallback.
- **`adapter/net/session.rs:892-912`** — `consumed`/`granted` two-step atomic; outstanding-pinned invariant holds only quiescent.
- **`adapter/net/behavior/proximity.rs:611`** — self-edge `insert_or_update_edge` per-pingwave is hot-path noise.
- **`netdb/db.rs:103-114`** — `close()` swallows the second adapter error on double failure.
- **`shard/mapper.rs:609`** — `overloaded_count` uses stale-metric placeholders for freshly-added shards.
- **`shard/mapper.rs:681`** — `next_shard_id.store(first_id + count)` lacks `checked_add`.
- **`shard/batch.rs:135`** — `f64 → usize` `as` cast without `clamp`.
- **`consumer/merge.rs:91`** — JSON cursor key `"00"` parses to `0`, colliding with shard 0.
- **`adapter/jetstream.rs:107`** — missing `r` field stores `b"null"`, may surprise downstream consumers.
- **`ffi/cortex.rs:443-450, 929-938`** — non-success paths don't write `*out_json`/`*out_len`; pre-zero is contract.
- **`ffi/mod.rs:1349-1358`** — `CString::new` failure reported as `InvalidUtf8` but actual cause is interior NUL.
- **`ffi/mod.rs:1192-1209`** — `NetEvent`/`NetReceipt` `#[repr(C)]` lacks cross-arch alignment pinning.
- **`ffi/cortex.rs:419-453`** — JSON serialization runs while `TokioMutex` held; per-cursor latency stall.
- **`adapter/net/compute/migration_source.rs:202-213` (`take_buffered_events`)** — no phase guard; misuse-prone.
- **`adapter/net/compute/migration_target.rs:362-368` (`abort`)** — `completed` index not cleared; minor leak.
- **`adapter/net/compute/orchestrator.rs:1052-1066`** — `MigrationError::TargetUnavailable(0)` invents a fake node id.
- **`adapter/net/compute/orchestrator.rs:1280-1293` (`buffer_event`)** — returns `false` at Cutover; downstream caller may route to source post-handoff.
- **`adapter/net/compute/migration.rs:139, 153`** — `started_at: u64` saturates on clock jump backward; use `Instant`.
- **`adapter/net/compute/fork_group.rs:225-231`** — `forks.pop()` and `coord.remove_last()` invariant unenforced; brittle.
- **`adapter/net/redex/file.rs:474-497`** — `next_seq` rollback skipped if `disk` is `None` (currently safe path).
- **`timestamp.rs:163`** — `raw_to_nanos(raw)` uses `delta_as_nanos(0, raw)`; quanta semantics unspecified.

## Audit notes

- Three subsystems are notably well-defended: `crypto.rs` (nonce/replay/HKDF/AEAD all sound), `pool.rs` (TX nonce coordination correct), and most `FailureDetector`/`CircuitBreaker` paths.
- `mesh.rs` (~7953 lines) was not fully read in this pass — only spot-checks. Worth a dedicated follow-up.
- The codebase has aggressive regression tests (`test_regression_*`) that pin many historical bugs; the findings above are not covered by those.
- Triage priority: #1, #2, #3 (Critical), then #4 (temp-file leak), #5–#7 (cortex watermark/checksum durability), #12 (`registry::replace`), #15 (FFI DoS).

## Follow-up pass — 2026-05-03

Second multi-agent sweep over `bus.rs`, `event.rs`, `timestamp.rs`, `shard/`, `consumer/`, `ffi/`, `adapter/` top-level, and `adapter/net/` top-level. New findings only — duplicates from the first pass omitted (e.g. `shard/mapper.rs:1024` finalize_draining ordering = existing #31).

### High

51. **`ffi/cortex.rs` (~30 sites: lines 207, 275, 323, 339, 354, 366, 388, 417, 487, 519, 571, 597, 622, 643, 667, 836, 871, 908, 970, 1000, 1062, 1103, 1132, 1154, 1175, 1196, 1390, 1421, 1453) — Handle-lifetime contract is unenforced.** Unlike `ffi/mod.rs`, the cortex FFI uses bare `&*handle` with no `FfiOpGuard`/`active_ops` analog. A caller that races `net_redex_free` against `net_redex_file_append` produces use-after-free: `Box::from_raw` in `_free` deallocates while another thread holds `&*handle`. The Go cgo SDK is exactly this multi-thread pattern. Fix: port the `active_ops` + `bus_taken` CAS protocol from `ffi/mod.rs`.

52. **`ffi/mesh.rs:498-505, 1131-1138, et al. — Same lack of shutdown/free synchronization across all mesh handles.** `net_mesh_free` / `net_mesh_stream_free` / `net_identity_free` / `net_redis_dedup_free` are unsynchronized vs concurrent uses of the same handle.

53. **`ffi/mesh.rs:1078-1079` — `MeshStreamHandle._node` keeps the node alive but not the handle box.** `_node: Arc<MeshNode>` pins the node, but `net_mesh_free(node_handle)` deallocates the handle's `Box` while `net_mesh_send` is dereferencing `&*node_handle` (e.g. through `handles_match` / `Arc::ptr_eq`). UAF on the handle pointer.

### Medium

54. **`bus.rs:1056-1106` (`flush` Phase 2) — Progress condition can return early under concurrent ingestion.** Gate uses the *global* `events_dispatched` counter; `target_ingested` is snapshotted at flush start, but `events_dispatched` keeps incrementing for events ingested *after* flush() began. A workload where post-flush traffic lands on shard A while pre-flush traffic sits in shard B's mpsc can satisfy `dispatched + dropped_during_flush ≥ target_ingested` while pre-flush events are still in flight. Defeats the documented delivery-barrier contract. Fix: snapshot `events_dispatched_at_start` and gate on the delta, or use per-event sequence numbers.

55. **`bus.rs:1437-1446` (`manual_scale_down`) — Leaks orphan shards into bus tables.** `mapper.finalize_draining()` returns *every* shard whose grace expired this tick. `manual_scale_down` only calls `remove_shard_internal` for IDs in its own `target` set, so any concurrently-finalized shard becomes `Stopped` while its workers, sender, and routing entry remain in `batch_workers` / `batch_senders` / routing tables.

56. **`ffi/mod.rs:1075-1081` — Concurrent `net_shutdown` callers race the `bus_taken` swap.** The drain protocol guards in-flight FFI ops but not concurrent shutdown callers themselves. A second/third caller can return `Success` while the first is still inside `runtime.block_on(bus.shutdown())` — falsely signaling completion of an in-progress shutdown. The doc comment "C contract guarantees not called concurrently with itself" admits this; nothing enforces it.

57. **`ffi/cortex.rs` and `ffi/mesh.rs` (every `runtime().block_on(...)` site: cortex.rs:419, 670, 910, 1199, 1455; mesh.rs:475, 639, 660, 676, 698, 810, 829, 902, 1209, 1242, 1277, 1360, 1689) — No `Handle::try_current()` guard.** `ffi/mod.rs:21-53` documents the contract; the cortex/mesh modules don't enforce it. A `block_on` panic unwinds across `extern "C"` → UB. `catch_unwind` shim is missing.

58. **`adapter/jetstream.rs:267-380` (`on_batch`) — Partial-failure can produce duplicate publishes.** Mid-batch publish error drops in-flight `PublishAckFuture`s but bytes are already on the wire; retry re-publishes. `Nats-Msg-Id` dedups *only within the dedup window* (default 2 min). Slow caller / long retry backoff lands the same event at two distinct seqs. Comment at 304-308 calls this "safe" but downstream `seq` consumers see duplicates.

59. **`adapter/jetstream.rs:336-371` — Async-cancellation hazard mid-loop.** No timeout wraps `publish_with_headers`. Tokio cancellation drops `acks` without awaiting; bytes already sent persist. Same wire/local-cancel split as #58. Compare `redis.rs:396` which does wrap.

60. **`adapter/redis.rs:316-405` — Pipeline timeout duplicate hazard documented but unfixed by default.** Lines 376-394 document the race. The `dedup_id` mitigation depends on the consumer running `RedisStreamDedup`, which is *not* wired up by default — Go consumers that skip the helper see duplicates.

61. **`adapter/net/mesh.rs:4191` — Lone non-saturating `hop_count += 1` on attacker-controlled byte.** Every other hop-count increment in the crate uses `saturating_add(1)` (`swarm.rs:122`, `route.rs:254`). Bounded today by the `< MAX_CAPABILITY_HOPS - 1` (15) guard above it, but one constant change from a debug-panic / release-wraparound on attacker-controlled input. One-line hardening.

### Lower-priority

- **`shard/batch.rs:73`** — `let window_start = now - self.config.velocity_window;` panics on `Instant - Duration` underflow. On Windows `Instant` is QPC-relative to boot; with a velocity_window of seconds and a process started immediately after boot, this aborts the batch worker task. Use `checked_sub`.
- **`shard/mapper.rs:875-904` (`drain_specific`)** — Doesn't bump `last_scaling`. `scale_down` always writes `last_scaling = Some(Instant::now())` (mapper.rs:976) so a follow-up `scale_up` is gated by cooldown; `drain_specific` removes a shard from Active without touching `last_scaling`, so `drain_specific(id) → scale_up(N)` bypasses the cooldown floor.
- **`bus.rs:2060-2064` (suspect)** — Drain worker `mem::replace` swaps `scratch` *before* the `sender.send(batch).await`. If the channel closes while the await is parked, the batch is silently dropped and the loop breaks — events never reach the adapter. Safe under documented shutdown ordering today; fragile to reorder (the activate-failure rollback path at 487-573 is the easy way to expose it).
- **`adapter/net/linux.rs:138-139` (suspect)** — `iov_base: packet.as_ptr() as *mut _` launders `*const u8` to `*mut` for `sendmmsg`. Sound (kernel reads only, `&[Bytes]` keeps storage alive) but the kind of pointer-provenance laundering Miri flags.
- **`adapter/net/pool.rs:198, 265`** — `(payload.len() - 16) as u16` truncation is safe under current `MAX_PAYLOAD_SIZE = 8112`, but unchecked. If anyone ever raises the cap past `u16::MAX + 16` without revisiting these lines, the wire `payload_len` silently truncates and the receiver mis-frames. Add `debug_assert!(payload.len() - 16 <= u16::MAX as usize)`.
- **`adapter/net/session.rs:644` (suspect)** — `verify_and_touch_heartbeat` doesn't pre-check `parsed.payload.len() == TAG_SIZE` before AEAD decrypt. AEAD catches the mismatch (wrong-length payload won't decrypt against the AAD-encoded `payload_len = 0`), but a cheap length check shortcuts cleartext-flood probes before they touch the cipher.

### Cleared (this pass)

`event.rs`, `timestamp.rs`, `consumer/{filter,merge,mod}.rs`, `shard/ring_buffer.rs`, `shard/batch.rs` core math, `adapter/dedup_state.rs`, `adapter/noop.rs`, `ffi/redis_dedup.rs`, plus most of `adapter/net/` (handshake, replay window, AEAD AAD, pool key-rotate, NACK descriptors, sack bitmap, fairness rotation) — all read in full this pass; no real defects beyond what's already in the existing audit. Regression tests pin the obvious classes.

Triage priority for this pass: **#51/#52/#53** (FFI UAF — exploitable by Go cgo callers without contract violation), then **#54** (silent flush-barrier violation), then **#58/#59/#60** (silent duplicates downstream).

## Follow-up pass — 2026-05-03 (third sweep)

Third multi-agent sweep over `bus.rs`, `event.rs`, `timestamp.rs`, `error.rs`, `config.rs`, `shard/`, `consumer/`, `ffi/`, `adapter/{jetstream,redis,redis_dedup,dedup_state,noop}.rs`, and `adapter/net/` (top-level files + spot-checks of subdirs). New findings only — duplicates of #1-#61 omitted (e.g. `block_on` panic in cortex/mesh = #57; equal-metric pingwave flap = #49; `MeshStreamHandle` UAF on freed node handle = #53).

### High

62. **`adapter/net/mesh.rs:2745` (`handle_routed_handshake` Case 2) — Routed-handshake replay nukes the live session, no rate limit.** Unconditionally overwrites `ctx.peers[peer_node_id]` with the fresh session derived from any inbound msg1; no in-progress-session guard, no `HandshakePacer`. Noise NKpsk0's responder uses a fresh ephemeral on each reply, deriving a brand-new session key per replay. A passive attacker who replays a captured msg1 replaces the legitimate session keys; the legitimate sender keeps the old keys, every subsequent packet fails AEAD and is dropped → trivial DoS of any node that ever handshook on a routed path. Fix: if a live session for `peer_node_id` already exists with the same `remote_static_pub`, drop the new handshake (or require it from the same `source`); add the same `HandshakePacer` the legacy adapter uses.

63. **`adapter/net/pool.rs:567-575, 597-720` (`ThreadLocalPool`) — Per-thread cache leaks forever.** No `Drop` impl removes the per-thread `LOCAL_BUILDERS[pool_id]` slot. Each new `NetSession` gets a fresh `pool_id` via `NEXT_POOL_ID`. Every connect/disconnect / NAT-rebind / mesh-rebuild cycle leaks ~16 KB × `local_capacity` × `num_threads`. Long-lived daemons OOM in proportion to lifetime peer-churn count. Fix: `impl Drop for ThreadLocalPool` walks every thread's `LOCAL_BUILDERS` to evict its `pool_id` slot (or trade the bare thread-local for a per-thread map weakly keyed off `Arc<ThreadLocalPoolInner>`).

64. **`bus.rs:545-570` (`add_shard_internal` rollback) — Stranded batch dispatched with stale `next_sequence` after worker-timeout.** On the worker-shutdown timeout path the JoinHandle is leaked (acknowledged in comment), but the next line still loads `workers.next_sequence.load(Acquire)` and dispatches a stranded batch — the still-running detached worker may not have published its final flush yet, so the rollback emits overlapping msg-ids with the detached worker. Adapter sees two batches claiming the same `sequence_start..sequence_end` range. Fix: on timeout, refuse to dispatch (or use a sentinel `sequence_start = u64::MAX/2`) to avoid colliding with the still-running worker.

65. **`ffi/mod.rs:570, 615, 670, 1253` (and similar in `ffi/mesh.rs`) — `slice::from_raw_parts(ptr, len)` lacks `len <= isize::MAX` validation.** Affects `net_ingest`, `net_ingest_raw`, `net_ingest_raw_batch`, `net_ingest_raw_ex`, plus `mesh.rs::collect_payloads`, `net_mesh_publish`, `net_redex_file_append`, `net_identity_sign`, `net_identity_install_token`, `net_parse_token`. A C caller passing a stray sign-extended `-1` (or any `len > isize::MAX as usize`) triggers immediate UB before any guard fires. Fix: reject `len > isize::MAX as usize` at every entry point before constructing the slice.

66. **`ffi/mod.rs:1134-1145, 1154-1160` — `net_generate_keypair` / `net_free_string` feature-gated, but `net.h` declares them unconditionally.** `net.h:266-271` exposes both with no feature gate; both Rust definitions are `#[cfg(feature = "net")]`. A consumer linking against a cdylib built without `net` gets load-time missing-symbol errors despite the header promising the symbol. Fix: add `#[cfg(not(feature = "net"))]` stubs (NULL-returning / no-op), mirroring the `nat-traversal` pattern at `mesh.rs:975-1058`.

67. **`adapter/redis.rs:503-520` (`is_transient_error`) — Cluster `MOVED` / `ASK` / `READONLY` / `CLUSTERDOWN` classified as fatal.** Substring set is `LOADING | BUSY | TRYAGAIN | MASTERDOWN`. After any Redis Cluster failover, every batch fails permanently until process restart. Fix: add `MOVED`, `ASK`, `READONLY`, `CLUSTERDOWN`, `NOREPLICAS` to the substring set; prefer redis-rs's `is_cluster_error()` / `is_io_error()` if available. Add unit tests pinning each substring against constructed `RedisError`s.

68. **`adapter/jetstream.rs:347-356` (`on_batch`) — "Pipelined" publish actually awaits each enqueue serially.** Loop `await`s `publish_with_headers` per event before moving on; only the server-ack `try_join_all(acks)` is parallel. On a 1ms-RTT link a 1k-event batch costs ~1s instead of the documented "~1 RTT per batch." Fix: push the publish-future itself into `acks` (no inner `.await`) and join, or use `js.send_publish(...)` (the explicitly non-awaiting variant).

69. **`adapter/jetstream.rs:104-123` (`poll_shard`) — Per-event `serde_json::Value` allocation violates "MUST NOT allocate per-event" contract.** `adapter/mod.rs:88` documents the contract; the Redis adapter (lines 150-171) correctly uses `RawValue` for zero-copy. JetStream parses the entire payload tree then re-serializes the `r` subtree per event. At 100k events/poll this is measurable; on a hot replay potentially OOM. Fix: mirror Redis's `RawValue` borrow + `Bytes::copy_from_slice(parsed.r.get().as_bytes())`.

70. **`adapter/jetstream.rs:537-644` (`poll_shard`) — One RTT per sequence in steady state.** `direct_get(seq)` per sequence — a 100-event poll on a 1ms-RTT link costs ≥100ms wall, bounded by latency not bandwidth. Fix: replace per-seq `direct_get` with `direct_batch_get` or a pull consumer's `fetch(N)` so one round-trip returns up to `fetch_limit` events.

### Medium

71. **`shard/mapper.rs:838-856` (`activate`) — `active_count.fetch_add` outside the held write-lock allows transient budget overshoot.** Sequence is `load → check → state-mutate → drop(shards) → fetch_add`. Two activates each pass the budget gate under their respective held locks; a third caller landing between either `drop(shards)` and the corresponding `fetch_add` reads the still-stale count and also passes. `active_count` transiently exceeds `max_shards`. Fix: move `self.active_count.fetch_add(1, Release)` *before* `drop(shards)` so the increment is published while the lock still serializes.

72. **`bus.rs:1200-1212, 1246-1274` (`shutdown_via_ref` and in-flight wait loops) — Bare `tokio::task::yield_now` thrashes the runtime.** `yield_now()` doesn't park; it just re-queues the task. Tight loop under contention starves the workers it's waiting on, prolonging shutdown. Fix: `tokio::time::sleep(Duration::from_millis(1)).await` (or backoff) in place of `yield_now`.

73. **`consumer/merge.rs:681-685` (`PollMerger::poll`) — Stalled poll loses cursor context.** `next_id` is `None` when no shards make progress, even if the caller passed a valid `request.from_id`. Caller may interpret `None` as "no events" and re-fetch from the beginning — silent regression of pagination across a stalled stream. Fix: when `request.from_id.is_some()` and no progress was made, echo back the original `from_id`.

74. **`bus.rs:1471-1528` (`Drop`) — Stranded events not surfaced in stats.** When the bus is dropped without `await shutdown()`, ring-buffer events are lost (acknowledged in comment) but `events_dropped` is never incremented and `shutdown_was_lossy` is never set. Operators reading post-mortem stats see no record of the loss. Fix: snapshot `shard_manager.shard_stats()` in `Drop`, increment `events_dropped` by each shard's ring-buffer length, set the flag.

75. **`adapter/net/failure.rs:297, 569, 692` (`FailureDetector::cleanup`, `CircuitBreaker`) — `unwrap()` on poisoned `std::sync::Mutex`.** The rest of the crate uses `unwrap_or_else(|p| p.into_inner())` (see `crypto.rs:691`). A single panic anywhere holding these locks turns every subsequent `unwrap()` into a runtime panic that takes down the failure-detection loop. Fix: switch to the `into_inner` pattern, or `parking_lot::Mutex` (already used elsewhere in the crate).

76. **`adapter/net/router.rs:461-546` (`route_packet`) — No source/loop suppression.** Decrements TTL based purely on `routing_header.dest_id`. No `routing_header.src_id == self.local_id` check (drop own packets coming back) and no LRU duplicate cache. Misconfiguration / `add_route_with_metric` flap / malicious peer can build a 2-hop loop only broken by TTL exhaustion — every looping packet wastes 2× bandwidth per hop. Fix: drop in `route_packet` when `routing_header.src_id == routing_table.local_id`; consider a small `(src_id, stream_id, sequence)` LRU.

77. **`adapter/net/linux.rs:110-197` (`BatchedTransport::send_batch`) — Silently truncates above `MAX_BATCH_SIZE = 64`.** `let total = packets.len().min(MAX_BATCH_SIZE)` then returns `Ok(<= 64)`. Caller compares against `packets.len()` to detect partial sends; a "successful" return of 64 hides that packets `[64..]` never reached the wire. Reliable streams already stashed those via `on_send` and only learn via NACK/RTO retransmit. Fix: return `io::Error::InvalidInput` when over the cap, or chunk internally and loop until exhausted.

78. **`adapter/net/config.rs:104` + `pool.rs:646-657` — `MAX_PACKET_POOL_SIZE = 1<<20` can OOM on first session.** `with_local_capacity` pre-allocates `size` builders × ~16 KB. At the configured ceiling that's ~16 GiB up-front per session — guaranteed OOM on a normal box. The cap was intended to *prevent* OOM. Fix: lower to a few thousand (default is 64), or lazy-allocate buffers on first use rather than in the pool constructor.

79. **`ffi/mod.rs:1409-1425` (`net_free_poll_result`) — Not idempotent, easy double-free.** Frees `events` and `next_id` but leaves the struct fields holding the freed pointers. C header just says "free a poll result" with no contract that the caller must null fields. A second call double-frees. Fix: after freeing, set `(*result).events = ptr::null_mut(); (*result).count = 0; (*result).next_id = ptr::null_mut();`.

80. **`ffi/redis_dedup.rs` (every `extern "C"` entry; representative for the crate-wide gap) — No `catch_unwind` shim.** With `panic = "unwind"` (Rust default), any panic inside an `extern "C"` body is UB across the cgo / N-API / cffi boundary. `redis_dedup` is the smallest demonstrable case but the same exposure exists everywhere. Partly overlaps with #57's `block_on` concern but applies to *all* panic sources, not only runtime-context conflicts. Fix: set `panic = "abort"` for the cdylib, or wrap every FFI body in `catch_unwind` returning a sentinel error code.

81. **`adapter/redis.rs:489-498` (`is_healthy`) — `tokio::time::timeout` cancellation desyncs `ConnectionManager`.** Same race the `on_batch` doc warns about (lines 376-394). When timeout fires, the in-flight PING future is dropped while bytes are still on the wire; a subsequent caller's response can be served the leftover PING reply, producing spurious "wrong type" errors on the next real command. Fix: dedicated health-check connection, or use `ConnectionManager`'s native timeout config.

82. **`adapter/jetstream.rs:475-535` (`poll_shard`) — Cold-stream bail enabled on transient `info()` failure.** On `info()` error, fallback uses `first_seq = 0`, which *enables* the cold-stream bail. A populated stream returning NotFound in a deletion gap then bails after 64 NotFounds; consumer believes the stream is drained when there are events ahead. Transient error converts a recoverable poll into silent under-delivery. Fix: propagate `AdapterError::Transient` rather than fabricating `first_seq = 0`.

83. **`adapter/jetstream.rs:236-242, 387-410` — Init-after-shutdown silently overwrites client without `drain()`.** `shutdown` clears `initialized = false`; `init` no-ops only on `initialized = true`. A reconnect path that calls `init` post-shutdown (or directly on a still-set `client`) overwrites `self.client` without first draining, losing in-flight publishes piggybacking on the prior client. Fix: in `init`, if `self.client.is_some()` and `initialized == false`, drain the prior client before overwriting; or document init-after-shutdown as unsupported.

Triage priority for this pass: **#62** (handshake-replay DoS — security), then **#65 / #66 / #79 / #80** (FFI footguns / UB), **#63** (production OOM), **#64** (duplicate delivery via stranded rollback), **#67** (full Redis Cluster outage on failover), then **#68 / #69 / #70** (JetStream perf/contract).

## Follow-up pass — 2026-05-03 (fourth sweep)

Fifth multi-agent sweep across `bus.rs`, `event.rs`, `config.rs`, `error.rs`, `timestamp.rs`, `lib.rs`, `shard/`, `consumer/`, `adapter/{mod,dedup_state,jetstream,noop,redis,redis_dedup}.rs`, `adapter/net/` top-level, all `adapter/net/*` subdirectories, and `ffi/`. New findings only — duplicates of #1–#83 omitted (e.g. `finalize_draining` `Relaxed` ordering = #31; `info()` cold-stream bail = #82; mid-batch publish `PublishAckFuture` drop = #58; Redis MULTI/EXEC dedup default capacity = #60; `extern "C"` panic surface = #80; `ThreadLocalPool` leak = #63; `TokenCache` slot-cap race = #32; `slice::from_raw_parts(p, len)` `isize::MAX` guard = #65).

### Critical

84. **`ffi/mod.rs:805-841` (`net_poll`) — Buffer-size check runs after the events have been consumed.** `bus.poll(request)` advances the consumer cursor *before* the response is serialized; if `response_json.len() + 1 > buffer_len`, the function returns `BufferTooSmall` and drops the entire `response`. The caller's contractual retry (re-call with a larger buffer) fetches starting from the now-advanced cursor — every event in the failed serialization is silently lost. The C-API doc itself shows fixed-size buffers, so the trigger is the recommended usage pattern, not a bug shape unique to misuse. Fix: serialize first, size-check before calling `poll`, or buffer the response so a retry can resume.

### High

85. **`bus.rs:1393-1400` (`manual_scale_up`) — Loop invariant violated whenever `cooldown > 0`.** Each iteration calls `add_shard_internal` → `mapper.scale_up_provisioning(1)`, which bumps `last_scaling = Instant::now()` on success and rejects with `InCooldown` while `last_scaling.elapsed() < cooldown`. Default `ScalingPolicy::default_for_cpus` has `cooldown = 30s`. Iteration 0 succeeds and stamps the timestamp; iteration 1 immediately fails. The first shard is left half-added (workers spawned, routing entry installed) while `?` propagates `InCooldown` to the caller — partial-state failure with no rollback. Fix: pass `count` through to a single `scale_up_provisioning(count)` call, or bypass the cooldown gate for explicit operator-initiated `manual_scale_up`.

86. **`adapter/jetstream.rs:387-410` (`shutdown`) — `self.jetstream` / `self.client` not cleared after `drain()`.** `shutdown` flips `initialized = false`, drains the client, and clears `streams` — but `self.jetstream = Some(...)` and `self.client = Some(...)` survive. `on_batch` checks `self.jetstream.as_ref()`, not `initialized`, so a post-`shutdown` `on_batch` proceeds against a drained client (typically erroring, sometimes hanging depending on async-nats internals). Fix: in `shutdown`, set `self.jetstream = None; self.client = None;` after drain completes, or have `on_batch` consult `initialized`.

87. **`adapter/redis.rs:421-426` (`shutdown`) — Pure advisory flag; `conn` retained.** Only flips `initialized = false`; `self.conn = Some(ConnectionManager)` is never cleared, and `get_conn` doesn't consult `initialized`. Subsequent `on_batch` calls write to Redis silently after the operator believes the adapter has shut down. No flush-before-return either. Fix: drop `self.conn` in `shutdown`, and have `get_conn` return `Err(Fatal)` when `initialized == false`.

88. **`adapter/jetstream.rs:602` (inner poll loop) — `Fatal` decode error discards already-decoded events without advancing the cursor.** When `deserialize_event` returns `Fatal` (e.g. shard_id out of range, corrupted `r` payload), the function returns immediately, dropping every event accumulated in `events` so far. The cursor never advances past the good prefix. After operator recovery the consumer re-polls from the previous cursor and re-emits the entire prefix — duplicate delivery. Fix: return `Ok(SubmitOutcome { events, next_id: Some(last_good_id), .. })` first, then surface the corruption on the next poll attempt.

89. **`adapter/jetstream.rs:327-365` (likely) — `(process_nonce, shard_id, sequence_start, i)` msg-id is unsafe across restart unless `sequence_start` is monotonic.** Persistent `process_nonce` (via `dedup_state`) ensures the prefix doesn't change across process restart, but the `(shard_id, sequence_start, i)` suffix must be globally unique-per-batch within the dedup window. If `Batch::sequence_start` ever resets per-restart (e.g. counter reinit instead of reading from `bus.events_dispatched()`), post-restart batches collide with pre-restart ones in the JS dedup window and the second batch is silently dropped server-side. Worth pinning a unit test that asserts `sequence_start` is monotonic across simulated bus restarts.

90. **`ffi/mod.rs:1304-1320` (`net_poll_ex`) — Allocation failure after `bus.poll` returns drops the entire batch.** Same shape as #84 for the structured API: `Layout::array::<NetEvent>(count)` and `std::alloc::alloc(layout)` failures both return `NetError::Unknown` and drop `response`. Caller sees a generic error, has no signal that data was lost, retries from the cursor that was already advanced. Fix: pre-validate `count` against a max event-count, or require the caller to pre-allocate the events buffer (turning this into a `BufferTooSmall`-style retryable error).

91. **`ffi/cortex.rs:186-188, 470-473, 956-958, 1402-1404` — `Arc<Inner...>`-wrapped FFI handles share state across threads with no compile-time `Send + Sync` audit.** `RedexFileHandle { inner: Arc<InnerRedexFile> }`, `TasksWatchHandle { stream: TokioMutex<Option<BoxStream<...>>> }`, etc. The handle is returned to C as `*mut HandleType` and routinely shared across goroutines / Python threads (the docstrings advertise this pattern). Soundness rests entirely on the inner type's auto-`Send + Sync` impl. A future refactor that adds a `Cell` / `RefCell` / `Rc` / `*mut` field to `InnerRedexFile` (or analog) compiles cleanly because the FFI layer doesn't typecheck `Send + Sync`, and any caller already sharing the handle across threads is suddenly racing — silent data race + UB. Fix: place `static_assertions::assert_impl_all!(InnerRedexFile: Send + Sync);` (and friends) next to each handle so refactors fail compile.

92. **`adapter/net/swarm.rs:599` (pingwave `strict_progress` predicate) — Address-poisoning via `hops < n.hops` arm.** `strict_progress = pw.seq > n.last_seq || hops < n.hops`. The OR lets an attacker who has observed pingwaves spoof `(origin_id=Y, seq=K, hop_count=0)` for a *not-yet-observed* `K` strictly less than `n.last_seq` (e.g. via `seen_pingwaves` eviction or a peer that joined late). The dedup filter at line 538 admits the entry, `hops=1 < n.hops=2` flips `strict_progress` true, and `n.addr` is overwritten to the attacker's UDP source. The existing regression test `on_pingwave_likely_restart_must_not_overwrite_addr` covers only seq-regression-with-equal-hops, so this path is unguarded. Fix: AND the conditions — require `pw.seq >= n.last_seq` *and* `hops <= n.hops` before honoring the address update.

93. **`adapter/net/compute/orchestrator.rs:1021-1027` (`start_migration` local-source path) — Always emits a single `SnapshotReady` regardless of size.** Returns `chunk_index: 0, total_chunks: 1` whether `snapshot_bytes` is 12 B or 12 MB. The wire encoder rejects any chunk larger than `MAX_SNAPSHOT_CHUNK_SIZE = 7000` (`migration_handler.rs:336`); the receiver-side reassembler also rejects via `ChunkTooLarge`. Any locally-initiated migration of a daemon whose serialized state exceeds 7 KB cannot be sent — easily hit by stateful daemons with cached models, large bindings, or non-trivial behavior history. Fix: route the local-source path through `chunk_snapshot(daemon_origin, snapshot_bytes, seq_through)` (already used by the remote path) and broaden the API to return `Vec<MigrationMessage>`.

94. **`adapter/net/contested/reconcile.rs:174-196` — `unreachable!()` is reachable on signed but divergent input.** The divergence loop at lines 105-117 enters the conflict branch on payload mismatch *or* `(parent_hash, sequence)` mismatch. When divergence is by parent-hash/sequence only and payloads happen to be byte-identical (e.g. after pruning + re-issue of the same payload under a different parent), the equal-length tiebreak at 174-196 falls into `Ordering::Equal` and panics the reconciliation thread. A malformed-but-signed remote chain crashes the worker. Fix: replace `unreachable!` with a deterministic tiebreak on `parent_hash` (or sequence), so the worst case is a one-sided resolution rather than thread death.

### Medium

95. **`adapter/redis.rs:458-466` (`poll_shard` XRANGE) — No `command_timeout` wrapper.** `on_batch` (line 396) and `is_healthy` (line 493) both wrap their pipelines in `tokio::time::timeout(self.config.command_timeout, ...)`; `poll_shard`'s XRANGE relies entirely on the `ConnectionManager`'s implicit timeout. On a slow or wedged Redis node, `poll_shard` can block indefinitely while the rest of the adapter is honoring the timeout contract. Fix: wrap the XRANGE call in the same `tokio::time::timeout` shell.

96. **`adapter/net/crypto.rs:483-494` (`shift_bitmap_up`) — Forward-jump > 1024 zeroes anti-replay state instead of refusing.** `MAX_FORWARD = 65_536` and `WINDOW_SIZE = 1024`. A single authenticated packet whose counter jumps `1025..65_536` past `rx_counter` is accepted by `is_valid`, the bitmap is zeroed, and any previously-seen counter inside `rx_counter - 1024 .. rx_counter` (whose "seen" bit just got dropped) becomes replayable. The warning log fires but the slide proceeds. Fix: refuse to slide more than `WINDOW_SIZE` and require a fresh handshake; or shrink `MAX_FORWARD` to `WINDOW_SIZE`.

97. **`adapter/net/session.rs:1153-1154` (`apply_authoritative_grant`) — Clamp to a racy `tx_bytes_sent` watermark truncates legitimate grants.** `try_acquire_tx_credit` decrements `tx_credit_remaining` (AcqRel CAS) *before* `fetch_add`-ing into `tx_bytes_sent`. The grant's `total_consumed.min(sent_watermark)` clamp Acquire-loads `tx_bytes_sent` and can observe a value that pre-dates an in-flight admission's `fetch_add`, while the receiver-reported `total_consumed` already reflects bytes from the same admission. The clamp truncates a legitimate grant; sender stalls briefly until the next grant unsticks it. Self-healing per the comment, but each truncation is a measurable send-side hiccup. Fix: bump `tx_bytes_sent` *before* decrementing `tx_credit_remaining`, or have grants observe `tx_credit_remaining` directly rather than reconstructing from `sent - max_consumed`.

98. **`adapter/net/session.rs:909-911` (`RxCreditState::on_bytes_consumed`) — `consumed` and `granted` are not jointly atomic.** Two threads each calling `on_bytes_consumed(100)` `fetch_add` independently; thread A's `consumed` store can be globally visible before its `granted` store. A concurrent `outstanding()` reader computes `granted - consumed` and transiently sees `consumed > granted`. `saturating_sub` masks it to 0, so admission is unaffected, but observability/metrics show transient zeroes during contention. Fix: take a single AcqRel CAS on a packed `u128` (consumed:64 | granted:64), or accept the metric blip and document it.

99. **`adapter/net/compute/orchestrator.rs:782-790` (snapshot reassembly `pending` map) — Unbounded chunk hold via repeated `seq_through == latest`.** Eviction at 782-790 only fires when the new chunk's `seq_through` is *strictly greater* than `latest_seq`. The reject gate at 770 only blocks `seq_through < latest`, so `==` is admitted. Combined with `MAX_TOTAL_CHUNKS = 700_000` × `MAX_SNAPSHOT_CHUNK_SIZE = 7000` per pending entry, an attacker (or a buggy peer) can park up to ~4.3 GiB of unfinished reassembly per `(origin, seq)` and refresh forever by re-sending the same `seq_through`. There's no per-peer cap, no time-based sweep. Fix: per-peer pending count + max-age eviction (e.g. 30 s without forward progress).

100. **`adapter/net/state/log.rs:88-99` (`EntityLog::append`) — Genesis-shaped event accepted with un-validated payload.** When `events.is_empty()` and `current_head.is_genesis()` and the new event's `link.is_genesis()`, the append is taken without any check on `event.payload` (any byte string passes). A peer-injected genesis-shaped event seeds the log with an attacker-chosen payload; divergence shows up only at the next event's parent-hash mismatch, but if no successor ever arrives, the log permanently carries an unverified anchor. Fix: pin a canonical genesis payload (often the empty vector) and reject anything else at this branch.

101. **`adapter/net/identity/token.rs:55-59` (`TokenScope::contains`) — `contains(NONE)` returns true unconditionally.** `(self.bits & NONE.bits) == NONE.bits` simplifies to `0 == 0`. Any token authorizes the no-op action; for any caller that builds an `action: TokenScope` from external input (e.g. a wire `u32` masked into a smaller subset and passed to `authorizes`) and the input happens to mask to `NONE`, the call returns `true` for *every* token. Compounds with `authorizes(NONE, ch)` returning unconditional `true`. Fix: short-circuit `if other == TokenScope::NONE { return false; }` at the top of `contains`, and reject `action == NONE` at every public ACL entry point.

102. **`adapter/net/identity/envelope.rs:361-396` (v0 fallback path) — Doubled AEAD CPU under attacker-shaped traffic.** Every `open` runs an AEAD decrypt with `aad_v1 = chain_link.to_bytes()`; on failure it retries with `aad = &[]` (the v0 envelope shape). Integrity is preserved by the outer signature covering `chain_link`, but the verifier pays two AEAD verifies per probe under any traffic that doesn't match v1 AAD — a passive attacker submitting random ciphertext can double the verifier's CPU per probe. Fix: pin a wire-format version byte at the start of the envelope so `open` selects v0/v1 deterministically.

103. **`ffi/mesh.rs:199-205` (`c_str_to_str`) — Lifetime elision binds returned `&str` to the `*const c_char` reference, not the underlying C buffer.** The signature `unsafe fn c_str_to_str(p: &*const c_char) -> Option<&str>` makes the borrow checker treat the result as if it lived as long as the local stack slot holding the pointer. Today's call sites are stack-only, but a future refactor that moves the result into a `tokio::spawn(async move { ... })` compiles silently and hands a dangling pointer to the spawned task. The neighboring `c_str_to_owned` already documents this hazard. Fix: rebind the lifetime explicitly via `unsafe fn c_str_to_str<'a>(p: *const c_char) -> Option<&'a str>` and force every caller to assert the lifetime they're claiming, or always copy into `String`.

104. **`ffi/mod.rs:662-680` (`net_ingest_raw_batch`) — Silent drop of null and invalid-UTF-8 entries.** A null entry pointer `continue`s; an invalid-UTF-8 entry is dropped. Function returns the number of accepted events without a per-entry error array. A binding that passed `[]string` with one garbled element sees `count - 1` accepted, attributes the drop to back-pressure, retries the wrong indices, and double-publishes the good ones. Compare the all-null `event_json` path which correctly returns `NullPointer`. Fix: accept an `out_failed_indices: *mut size_t, out_failed_len: *mut size_t` pair to surface the exact dropped indices.

105. **`ffi/mod.rs:180-186, 1048-1053` — `bus_taken` defense-in-depth claim is doc-only.** The doc on `bus_taken` says "FFI ops also check this before touching `bus`/`runtime`," but a grep finds the field read only inside `net_shutdown`. Soundness today is rescued by `shutting_down` + `active_ops`, but the doc-vs-code gap is a future-refactor trap (e.g. a "restart bus" RFE that resets `shutting_down` without auditing the `bus_taken` claim). Fix: either gate every `enter_ffi_op` against `bus_taken` to make the doc match, or remove the misleading sentence.

### Lower-priority

- **`shard/mod.rs:456-462`** — Static-mode `select_shard_by_hash` uses raw modulo while dynamic-mode (`mapper.rs:549`) was converted to Lemire's unbiased `(hash * len) >> 64`. Same bias, same fix should apply to both paths.
- **`shard/batch.rs:142`** — `current_batch_size * 3 + target` overflow. `BatchConfig::validate` doesn't bound `max_size` from above; debug build panics, release wraps. Default `max_size = 10_000` is fine; latent under hostile config. Add `max_size <= 1_000_000` to `BatchConfig::validate`.
- **`event.rs:148, 255, 471`** — `RawEvent::from_value` / `InternalEvent::from_value` / `StoredEvent::from_value` use `serde_json::to_vec(...).expect("Value serialization is infallible")`. Holds for `serde_json::Value` today, but the panic crosses into bus-level paths where the caller has reasonable expectations of fallibility. Defense-in-depth: return `Result`.
- **`adapter/net/router.rs:486-522`** — `RouterError::TtlExpired` recheck after `forward()` calls both `record_in` and `record_drop`, double-counting per-stream stats. Fix: skip `record_in` until after the post-decrement TTL check, or `record_drop` only.
- **`adapter/net/transport.rs:341-353`** — `BatchedPacketReceiver` thread spins at 1 ms intervals on persistent socket errors (bad fd, permission revoke). No exponential backoff, no break on hard errors. Add `EBADF` / `ENOTSOCK` early return.
- **`adapter/net/crypto.rs:441-453` (`ReplayWindow::commit`)** — A first authenticated packet with `received == u64::MAX` saturates `rx_counter` to `u64::MAX` (after zeroing the bitmap) and rejects every subsequent counter. Permanent receive-path poisoning from a single hostile authenticated packet (e.g. peer key compromise). Reject `received == u64::MAX` at `is_valid` instead.
- **`adapter/net/cortex/watermark.rs:100-101` (`WatermarkingFold`)** — A peer publishing `seq_or_ts == u64::MAX` saturates our `app_seq` to `u64::MAX`; the next legitimate ingest's `fetch_add(1)` either panics in debug or wraps to 0 in release, breaking per-origin monotonicity. "2^64 ingests is impossible" is true for honest writers, false for a hostile peer. Cap inputs at `u64::MAX - 1`.
- **`adapter/net/continuity/observation.rs:108-119` (`divergence_from`)** — `seq_diff_sum` u64 overflow silently in release with many entities × large sequence drifts. Use `saturating_add`.
- **`adapter/net/continuity/chain.rs:343-348` (`assess_continuity`)** — `(0, Some(non-empty payload))` accepted as genesis-shaped without rejection; downstream parent-hash is computed against junk and the chain is reported `Forked` rather than rejected with a typed error. Add `if sequence == 0 && payload != &[] { return Unverifiable }`.
- **`adapter/net/contested/correlation.rs:235-244`** — Capability-index parent walk loops forever if `SubnetId::parent` ever returns a non-self, non-global subnet that loops back. Defensive depth cap (4 iterations matches the 4-level hierarchy).
- **`ffi/mesh.rs:2447-2449`** — `g.fp16_tflops_x10.map(|tf| tf as f32 / 10.0)` is lossy for `u32` values ≥ 2²⁴; the neighboring `tops_x10` already adopted `saturating_u16_cap` for the same reason. Apply matching saturation here.
- **`ffi/mesh.rs:2279-2290` (`parse_modality_cap`)** — Unknown modality strings silently fall back to `Modality::Text`; used for both capability announcements and capability filters. A typo in `require_modalities` returns wrong nodes with no error. Compare `parse_visibility` (returns `Option`) and `cortex.rs:787, 1330` filter parsers (reject unknown). Switch to `Option<Modality>` and surface `NET_ERR_CHANNEL` on unknown.
- **`adapter/dedup_state.rs:169`** — Startup nonce is `xxh3_64` of a `(pid, tid, ns, stack_addr_as_usize, ...)` mix. Non-cryptographic; the comment dismisses collisions as "good enough in practice," but on 32-bit targets the `as usize` capture of stack address narrows entropy. Two co-located pods restarting from the same checkpoint at the same wall-clock instant carry tighter margins than the comment suggests. Use a 128-bit hash or a /dev/urandom seed mixed in.

Triage priority for this pass: **#84** (silent data loss on every `BufferTooSmall`), then **#85** (broken `manual_scale_up` API on default config), **#86 / #87** (post-shutdown writes), **#88 / #89** (JetStream duplicate / silent-drop hazards on the recovery path), **#92** (pingwave address poisoning — security), **#93** (locally-initiated migration broken for any non-trivial daemon state), **#94** (reconciliation worker crash), then **#90 / #91** (FFI data loss + `Send+Sync` audit), **#95 / #96** (timeout / replay-window robustness), **#101** (TokenScope ACL footgun).
