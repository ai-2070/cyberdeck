# Bug Audit — 2026-05-02

Scope: `net/crates/net/` (477 Rust files). Five parallel hunters scanned by functional area:
cortex, channel/redex, compute/traversal/contested/continuity, sdk/bindings, and
state/subnet/behavior/identity/netdb/config. Findings consolidated and ranked below.

Severity ladder:
- **Critical** — panic/corruption/security failure, no caller-visible signal
- **High** — incorrect behavior on a realistic path
- **Medium** — edge case or contention-only failure
- **Low** — latent / footgun / by-design / astronomical

---

## Critical / High

### Lost events on partial replay failure
**File:** `src/adapter/net/compute/migration_target.rs:400-426`
```rust
while let Some(event) = state.pending_events.remove(&next_seq) {
    to_replay.push(event);   // events removed BEFORE delivery
    next_seq += 1;
}
...
for event in &to_replay {
    self.daemon_registry.deliver(...)?;   // early-return on failure
}
if let Some(last) = to_replay.last() { state.replayed_through = last.link.sequence; }
```
**Failure:** If `deliver()` fails on event N of M during `replay_events` / `buffer_event`,
the `?` returns early. All events in `to_replay` were already removed from
`pending_events`, but `replayed_through` is never updated. On retry, those events are
gone. Daemon state is permanently corrupted. **Severity: high.**

### Silent eviction during tail backfill (no `Lagged`)
**File:** `src/adapter/net/redex/file.rs:820–892`
```rust
let start = state.index.partition_point(|e| e.seq < from_seq);
let backfill_count = state.index.len() - start;
```
**Failure:** A subscriber requesting `from_seq=0` after retention has dropped the head
gets only the currently retained tail with no `Lagged` signal — `partition_point`
returns the first retained entry with `seq >= from_seq`, so the "history was lost"
condition is never detected. Documented behavior on `read_range` but `tail`'s doc
explicitly promises `Lagged` on backfill overflow only. Should also fire on
seq-below-retained. **Severity: high (silent data loss for resume-from-snapshot
consumers).**

### Index task exits permanently after `Lagged`
**File:** `src/adapter/net/redex/index.rs:131–139`
```rust
Some(Err(e)) => { tracing::debug!(error = %e, "RedexIndex tail error; continuing"); }
```
**Failure:** `RedexError::Lagged` is logged at `debug` and the loop continues. After
`Lagged` the underlying watcher is dropped from the file, so the next `tail.next()`
yields `None` and the index task exits permanently with the index permanently
incomplete. Keys missing post-`Lagged` stay missing forever. **Severity: high (silent
index corruption).**

### Node binding `u32 → u8` truncation on member index
**File:** `bindings/node/src/groups.rs:664, 672`
```rust
pub fn member_role(&self, index: u32) -> Option<String> {
    self.inner.member_role(index as u8)...
pub fn synced_through(&self, index: u32) -> Option<BigInt> {
    self.inner.synced_through(index as u8)...
```
**Failure:** JS caller invokes `group.memberRole(256)` expecting `null` (out-of-range).
`as u8` wraps to `0`, so it returns the role for member 0. Same for `syncedThrough` —
caller polling sync progress for index 257 silently gets the lag of member 1, makes an
incorrect failover decision. The crate already has a `count_u8()` helper for this —
these two calls bypass it. **Severity: high.**

### `NetBuilder::identity()` silently overrides `entity_keypair`
**File:** `sdk/src/config.rs:160-165`
```rust
if let (Some(id), AdapterConfig::Net(ref mut net_cfg)) =
    (self.identity.as_ref(), &mut adapter)
{
    net_cfg.entity_keypair = Some((**id.keypair()).clone());
}
```
**Failure:** A user calls
`.mesh(NetAdapterConfig::initiator(...).with_entity_keypair(kp_a)).identity(id_b)`
(in any order — `mesh()` doesn't take `&mut`). Both inputs are caller-supplied
keypairs; no error is raised, no precedence is documented, `id_b` silently wins. The
two identities are loadbearing for `node_id` / `entity_id` — picking the wrong one
routes the node under the wrong identity. **Severity: high — silent contract violation
on cryptographic identity selection.**

### Python bindings hold GIL across blocking compute ops
**File:** `bindings/python/src/groups.rs:257-272, 354-366, 464-476`
```rust
fn scale_to(&self, n: u8) -> PyResult<()> { self.inner.scale_to(n).map_err(group_err) }
fn on_node_failure(&self, failed_node_id: u64) -> PyResult<Vec<u8>> { ... }
fn sync_standbys(&self) -> PyResult<u64> { ... }
fn promote(&self) -> PyResult<u32> { ... }
```
**Failure:** Module docstring promises sync because "PyO3's `Python::attach` is safe
from any tokio worker." But `scale_to`, `on_node_failure`, `promote`, `sync_standbys`
invoke scheduler placement, registry operations, snapshot serialization — all of which
can block on internal locks for ms–s. None wrap with `py.allow_threads(...)`. A Python
program with a watchdog thread or asyncio loop is frozen for the duration. **Severity:
high.**

### Lossy `as_f64` for all numeric ordering in rules
**File:** `src/adapter/net/behavior/rules.rs:118-120`
```rust
let a = a.as_f64()?;
let b = b.as_f64()?;
a.partial_cmp(&b)
```
**Failure:** Rule conditions `Lt`/`Gt`/`Le`/`Ge` against `serde_json` u64/i64 fields
>2^53 (e.g., timestamps in ns, byte counts) silently truncate. Two adjacent values
like `9_007_199_254_740_992` and `9_007_199_254_740_993` compare as `Equal`; a `Gt`
guard on a quota or sequence number returns `false` and the rule fails to fire. NaN /
Infinity (legal under arbitrary_precision) yield `None`, silently masking the bug.
**Severity: high.**

### Snapshot-store retention drops high-water mark on `remove`
**File:** `src/adapter/net/state/snapshot.rs:592-594`
```rust
pub fn remove(&self, entity_id: &EntityId) -> Option<StateSnapshot> {
    self.snapshots.remove(entity_id.as_bytes()).map(|(_, s)| s)
}
```
**Failure:** A stale producer racing a retention-`remove` re-stores an older snapshot
at lower `through_seq` than the just-removed one — concrete ABA / state rollback. The
regression test `cr17_remove_does_not_carry_forward_through_seq_high_water_mark` pins
this as a documented limitation; multi-producer paths can hit it without the locking
the test enforces. **Severity: high.**

### Untrusted-input panics in subnet config
**Files:**
- `src/adapter/net/subnet/assignment.rs:73-77, 124-128`
- `src/adapter/net/subnet/id.rs:43-48`
```rust
assert!(rule.level < 4, ...);          // SubnetPolicy::add_rule
assert!(level_value != 0, ...);        // SubnetRule::map
assert!(levels.len() <= MAX_DEPTH ...) // SubnetId::new
```
**Failure:** Subnet policies typically come from config / FFI / JSON. A config file
with `level: 4` or `level_value: 0` or `>4 entries` panics the daemon loader. No
fallible constructor exists. **Severity: high.**

### `SdkError::Ingestion(String)` flattens structured `IngestionError`
**File:** `sdk/src/error.rs:85-89`
```rust
impl From<net::error::IngestionError> for SdkError {
    fn from(e: net::error::IngestionError) -> Self {
        SdkError::Ingestion(e.to_string())
    }
}
```
**Failure:** `IngestionError` has structured variants (`Backpressure`, `Sampled`,
`Unrouted`, shutdown). The SDK already exports `SdkError::Backpressure` and
`SdkError::Shutdown` with rich semantics for stream-side errors, but ingestion-side
mappings stringify everything. A user calling `node.emit(...)` on a backpressured bus
gets `SdkError::Ingestion("backpressure: ring buffer full")` and must string-match to
distinguish from `Sampled` (event deliberately dropped, retry pointless) or `Unrouted`
(topology transient). **Severity: high — looks ergonomic but forces wrong remediation.**

---

## Medium

### Observable seq rollback via `next_seq()`
**File:** `src/adapter/net/redex/file.rs:393, 440, 509, 608, 648, 706`
```rust
let seq = self.inner.next_seq.fetch_add(1, Ordering::AcqRel);
// ... disk write ...
self.inner.next_seq.fetch_sub(1, Ordering::AcqRel); // on disk failure
```
A reader calling `RedexFile::next_seq()` (line 358 — `Ordering::Acquire` outside the
state lock) can observe the temporarily incremented value before the rollback completes
on disk failure. Window where external observers (metrics, snapshot logic, an
`IndexStart::FromSeq(next_seq())` re-tail) believe a seq exists that was never durably
appended.

### Disk I/O held under `parking_lot::Mutex`
**Files:** `src/adapter/net/redex/file.rs:475–565, 678–758, :987`
`append_batch` calls `disk.append_entries_at(...)` and `sweep_retention` calls
`disk.compact_to(...)` while holding `state.lock()` (a non-yielding parking_lot
Mutex). All concurrent appenders, `tail` registrations, `read_range`, `len`,
`is_empty`, and `close` block on the same lock during disk I/O. Latency / starvation,
not correctness.

### `ingest_typed` post-`open` race vs foreign same-origin writers
**Files:**
- `src/adapter/net/cortex/memories/adapter.rs:116-117`
- `src/adapter/net/cortex/tasks/adapter.rs:119-120, 340-341`
```rust
let file = redex.open_file(&name, redex_config)?;
let next_seq = file.next_seq();
```
Concurrent ingests on this channel can append events between `CortexAdapter::open`
(which spawned the fold task with `file.tail(start_seq)`) and `next_seq()` here.
`wait_for_seq(next_seq - 1)` then waits for a moving target — there is a window where
post-read appends with our `origin_hash` (e.g. crashed sibling adapter resumed) can
land *after* `wait_for_seq` returns yet *before* the user's first `ingest_typed`, and
collisions can occur. Doc-comment on `MemoriesAdapter::open` overstates the guarantee.

### DashMap entry guard held across registry I/O
**File:** `src/adapter/net/compute/migration_source.rs:73-101`
```rust
let entry = match self.migrations.entry(daemon_origin) { ... Vacant(entry) => entry };
if !self.daemon_registry.contains(daemon_origin) { return Err(...); }
let snapshot = self.daemon_registry.snapshot(daemon_origin)?...;
entry.insert(...);
```
The DashMap shard write guard from `entry()` is held across `daemon_registry.snapshot()`,
which itself locks an inner `Mutex<DaemonHost>`. Combined with another caller acquiring
the daemon-host mutex first then touching this dashmap shard, deadlock risk. Also,
`take_snapshot` may run user code (`MeshDaemon::snapshot()`) under the held shard
guard, blocking concurrent migrations on co-hashed origins.

### Non-deterministic verdict ordering
**File:** `src/adapter/net/contested/correlation.rs:158-164`
```rust
let window_failures: Vec<u64> = self.recent_failures.iter().map(|e| e.node_id)
    .collect::<std::collections::HashSet<_>>().into_iter().collect();
```
`HashSet<u64>` iteration order is randomized per process. `window_failures` is the
`failed_nodes` list passed verbatim into `PartitionRecord::other_side`
(`partition.rs:160`). Two nodes with identical inputs assemble different `other_side`
orderings, causing divergent serialization, divergent reconcile ordering, and replays
that can't be cross-validated.

### Stream tight-loop spin
**File:** `sdk/src/stream.rs:158, 174-185`
```rust
this.current_interval = (this.current_interval * 2).min(this.opts.max_backoff);
let mut sleep = Box::pin(tokio::time::sleep(this.current_interval));
match sleep.as_mut().poll(cx) {
    Poll::Ready(()) => { cx.waker().wake_by_ref(); Poll::Pending }
```
`SubscribeOpts::default().poll_interval(Duration::ZERO).max_backoff(Duration::ZERO)`
with no events resolves the zero-duration sleep instantly, immediately re-wakes, and
hammers `bus.poll()` at 100% CPU. No validation rejects zero.

### `record_execution` window-reset across rule reload
**File:** `src/adapter/net/behavior/rules.rs:1184-1200`
```rust
state.last_execution = Some(now);
state.execution_count += 1;
...
if now.duration_since(state.window_start) >= window_duration {
    state.window_start = now;
    state.execution_count = 1;
}
```
`record_execution` is called for rules whose *current* `rate_limit` is `None`
(`if let Some(ref limit)` guards only the reset, not the increment). A rule that
toggled rate-limited → unlimited carries its old `execution_count` forever; on toggle
back with the same window, it is immediately blocked because `count >= max`. Concrete
on hot-reload of a rule's limits.

### `NetAdapterConfig::validate` accepts pathological values
**File:** `src/adapter/net/config.rs:230-249`
Only zero/ordering checks; `packet_pool_size = usize::MAX`,
`handshake_retries = usize::MAX`, `heartbeat_interval = 1ns`, `session_timeout = 2ns`
all pass. A 2-byte typo (`with_pool_size(1_000_000_000)`) walks past validation and
OOMs at first allocation.

### `StreamWindow.decode` unbounded `total_consumed`
**File:** `src/adapter/net/subprotocol/stream_window.rs`
Codec accepts any `total_consumed: u64`. Doc-comment formula
`tx_credit_remaining = tx_window - (tx_bytes_sent - total_consumed)` underflows /
wraps if peer sends `total_consumed > tx_bytes_sent`. The codec's promise of
"self-healing" gives a false sense of safety if the consumer doesn't clamp — verify in
caller.

### Partition `next_id` reuses ID 0 on `u64::MAX` overflow
**File:** `src/adapter/net/contested/partition.rs:153-154`
```rust
let id = self.next_id;
self.next_id += 1;
```
At `u64::MAX`, panics in debug, wraps in release reusing ID 0 — `confirm`/`get` could
operate on the wrong record. Astronomical in practice.

### `Rule::new` lossily casts `u128` millis to `u64`
**File:** `src/adapter/net/behavior/rules.rs:579, RuleSet::new:1237`
```rust
.as_millis() as u64;
```
Silent truncation past year ~584,554,051 — not operational. `as` cast on
`unwrap_or_default()`'d `Duration::ZERO` (clock pre-epoch / VM clock-skew) silently
produces `created_at = 0`, which then collides across rules and breaks any
"newer-rule-wins" logic.

---

## Low

### `ttl_seconds = 0` token mints expired
**Files:**
- `bindings/node/src/identity.rs:222`
- `bindings/python/src/identity.rs:221`
- `sdk/src/identity.rs:139`
Caller mints a token with TTL 0; `not_after == not_before` so it's expired before it
leaves the call. Token verifies signature-OK but every check fails at the receiver. No
diagnostic.

### Age-retention off-by-one
**File:** `src/adapter/net/redex/retention.rs:75`
```rust
let cutoff = now_ns.saturating_sub(max_age_ns);
...
if ts > cutoff { break; }
age_drop += 1;
```
An entry with `ts == cutoff` is dropped (the condition is `ts > cutoff`); should be
`>=` to retain the boundary.

### `new_heap` accepts `RedexFlags::INLINE`
**File:** `src/adapter/net/redex/entry.rs:71`
```rust
flags_and_checksum: (flags & FLAG_MASK) | (checksum & CHECKSUM_MASK),
```
A caller passing `RedexFlags::INLINE` to `new_heap` produces an entry whose
`is_inline()` returns true but whose `payload_offset`/`payload_len` are real heap
fields rather than payload bytes. `materialize` would then invoke `inline_payload()`
and reinterpret the offset/length as data — silent corruption. Currently not
exercised (callers all pass `flags=0`), but unguarded footgun.

### Channel decoder accepts trailing bytes on UNSUBSCRIBE/ACK
**File:** `src/adapter/net/channel/membership.rs:218-219`
For both `MSG_UNSUBSCRIBE` and `MSG_ACK` the decoder returns `Ok` without checking
`cur.remaining() == 0`. Pre-existing tests enforce strict-trailer rejection only on
the SUBSCRIBE path (regression test at line 309). A malformed peer can append
arbitrary bytes after a valid `Unsubscribe` or `Ack`. Defense-in-depth, not
exploitable.

### `Publisher::all_delivered()` returns false for empty roster
**File:** `src/adapter/net/channel/publisher.rs:130`
```rust
self.attempted > 0 && self.delivered == self.attempted && self.errors.is_empty()
```
A publish to a channel with zero subscribers is `is_empty() == true` AND
`all_delivered() == false`. Callers branching on `all_delivered()` for "all good"
treat empty rosters as failure. Common bug after `remove_peer` GC.

### `append_batch` empty-input returns plausible-looking seq
**File:** `src/adapter/net/redex/file.rs:475–479`
```rust
if payloads.is_empty() {
    return Ok(self.inner.next_seq.load(Ordering::Acquire));
}
```
Returns the next-to-assign seq for an empty batch; caller cannot distinguish "wrote
zero entries" from "wrote one entry with that seq." Footgun.

### `condition_count` off-by-one for `Not`
**File:** `src/adapter/net/behavior/rules.rs:294`
```rust
ConditionExpr::Not(c) => c.condition_count(),
```
The `Not` node itself is dropped from the count — a `Not(big_or)` evades a per-rule
condition cap by 1. Concrete only if a caller enforces a hard cap with this method.

### `OriginStamp` has no per-packet binding
**File:** `src/adapter/net/identity/origin.rs:42-46`
`origin_hash` is written verbatim into headers; tamper resistance is delegated to
AEAD. Any peer with the session key can mint packets claiming arbitrary `origin_hash`
values within the channel — by-design ("zero per-packet crypto") but worth flagging
since the audit explicitly looked at origin spoofability.

### `Stop` policy halts without final `changes_tx` notify
**File:** `src/adapter/net/cortex/adapter.rs:516-525`
```rust
match policy {
    FoldErrorPolicy::Stop if !recoverable_decode => true,
    ...
}
```
When `Stop` halts on a non-recoverable error, the loop breaks without emitting any
`changes_tx.send` for that seq. Subscribers using `changes_with_lag()` get no terminal
signal — they only learn the task halted by separately polling `is_running()`.
Documented but UX rough.

### Migration `next_seq` overflow
**File:** `src/adapter/net/compute/migration_target.rs:398`
```rust
let mut next_seq = state.replayed_through + 1;
```
If `replayed_through == u64::MAX`, `+1` panics in debug (wraps in release, immediately
mis-keys lookups). Astronomical.

### `on_node_recovery` does not break after first matching partition
**File:** `src/adapter/net/contested/partition.rs:185-213`
A node that simultaneously appears in `other_side` of two overlapping partition
records is added to BOTH `reappeared` lists, and BOTH may transition to `Healed` from
one recovery signal. If healing actions fire per partition, the recovery is
double-counted. Overlapping partitions are atypical but not prevented.

### Node-binding groups carry an unused `kind: String` field
**File:** `bindings/node/src/groups.rs:399, 488, 576`
The `kind` is captured at spawn but never read — `Arc<SdkReplicaGroup>` holds its own
copy. Trivial waste, but a maintenance trap: anyone who later "uses" `self.kind` will
read a snapshot that can drift from the SDK-side kind.

### Stream backoff overflow on absurd `poll_interval`
**File:** `sdk/src/stream.rs:158`
`this.current_interval * 2` panics on `Duration` overflow. Not realistic for sane
inputs, but the SDK accepts any `Duration` from `poll_interval()`.

---

## Top 3 worth fixing first

1. `migration_target.rs` lost-events on `?` early-return — silent permanent desync.
2. `redex/file.rs` silent backfill below retention — silent data loss for
   resume-from-snapshot consumers.
3. `redex/index.rs` permanent index halt after `Lagged` — silent index corruption,
   keys never reappear.

All three cause silent data divergence with no caller-visible signal.

---

## Follow-up Pass — Core / Adapter / FFI (2026-05-02)

Second audit pass covering areas the original sweep didn't focus on: top-level
`src/` (bus, shard, consumer), `adapter/jetstream.rs`, `adapter/redis*.rs`,
`adapter/dedup_state.rs`, `adapter/net/pool.rs`/`crypto.rs`/`protocol.rs`, and
`ffi/`. Four parallel hunters; high-severity claims spot-checked against source.

### High

#### `ShardManager::activate_shard` double-counts on idempotent calls
**File:** `src/shard/mod.rs:792-800` (with `src/shard/mapper.rs:760-806`)
```rust
pub fn activate_shard(&self, shard_id: u16) -> Result<(), ScalingError> {
    let mapper = ...;
    mapper.activate(shard_id)?;          // returns Ok(()) early when already Active
    self.num_shards
        .fetch_add(1, std::sync::atomic::Ordering::Release);  // unconditional
    Ok(())
}
```
**Failure:** Docstring at line 791 promises "Idempotent: calling on an already-`Active`
shard is `Ok(())`." The mapper honours this — when state is already `Active` it
returns `Ok(())` at `mapper.rs:766` without bumping its own `active_count`. But
`ShardManager` then unconditionally `fetch_add(1)`s `num_shards`. After repeated
activate calls, `num_shards` exceeds the mapper's `active_count` and the actual
shard count, breaking modulo-based shard selection (`select_shard`) and producing
stale routing decisions. **Severity: high.**

#### Silent `event_count` truncation in packet builder
**File:** `src/adapter/net/pool.rs:144, 208`
```rust
let header = NetHeader::new(
    self.session_id, stream_id, sequence, [0u8; NONCE_SIZE],
    self.payload.len() as u16,
    events.len() as u16,            // silent wrap if > 65_535
    flags,
)
```
**Failure:** Protocol-level cap is `MAX_EVENTS_PER_PACKET = 2027`
(`protocol.rs:443`), but the packet builder doesn't enforce it before the cast.
A caller passing >65 535 events wraps to `len % 65536`. The receiver's `validate()`
(`protocol.rs:451`) accepts any `event_count <= 2027`, so a wrapped value like
34 464 fails validation noisily — but a wrapped value below 2027 (e.g. caller passed
67 562 events) passes validation, and the receiver mis-parses the payload because
the actual event_count on the wire mismatches what's encoded. Add
`debug_assert!(events.len() <= MAX_EVENTS_PER_PACKET as usize)` plus a runtime guard
returning an error. **Severity: high (silent corruption).**

### Medium

#### Anti-replay window cleared on large legitimate jumps
**File:** `src/adapter/net/crypto.rs:417, 469-476`
```rust
fn is_valid(&self, received: u64) -> bool {
    if received >= self.rx_counter {
        received.saturating_sub(self.rx_counter) <= Self::MAX_FORWARD  // 65_536
    } ...
}
fn shift_bitmap_up(&mut self, shift: u64) {
    ...
    if shift >= (Self::BITMAP_WORDS as u64) * 64 {  // 1024
        self.bitmap = [0; Self::BITMAP_WORDS];      // wipes prior tracking
        return;
    }
    ...
}
```
**Failure:** `MAX_FORWARD = 65 536` allows accepting future counters far beyond
`WINDOW_SIZE = 1024`. When a legitimate packet jumps the counter forward by more
than 1024, `shift_bitmap_up` zeros the bitmap, leaving the prior ~64 000 counters
unprotected. Any captured packets in that window can be replayed undetected. Either
tie `MAX_FORWARD` to `WINDOW_SIZE`, or document the tradeoff as accepted.
**Severity: medium (replay window of seen packets).**

#### `pushes_since_drain_start` mismatched atomic ordering
**File:** `src/shard/mapper.rs:213-215` (read), `:203-204` (writer)
The reader getter uses `Ordering::Relaxed`; the writer (`set_draining(true)`) uses
`SeqCst` for the reset. On weakly-ordered hardware (ARM), `finalize_draining` can
read a stale counter and falsely conclude the drain has flushed while producers are
still pushing. Reader should be `Acquire` to pair with the release of the reset.
**Severity: medium (architecture-specific).**

#### Silent `shard_id` truncation in JetStream deserialize
**File:** `src/adapter/jetstream.rs:110`
```rust
let shard_id = value.get("s").and_then(|v| v.as_u64()).unwrap_or(0) as u16;
```
A stored `shard_id` of 100 000 silently becomes 34 464 (`100_000 % 65_536`). No
warning. Mis-routes events under the wrong shard at consume time. Either reject the
message or log a deser error; better, store the value as the same width it was
written with.

#### `dedup_state` silently swallows fsync failures
**File:** `src/adapter/dedup_state.rs:221-235`
```rust
let _ = f.sync_all();
```
Disk-full or I/O errors during `sync_all` are discarded. The producer-nonce file
may be partially persisted; the next restart resumes with a stale nonce, defeating
the cross-restart dedup guarantee that's the entire point of the file. Propagate
the error (or at minimum log at warn).

#### `Redis::is_healthy` PING has no enforced timeout
**File:** `src/adapter/redis.rs:465-478`
```rust
async fn is_healthy(&self) -> bool {
    if !self.initialized.load(Ordering::Acquire) { return false; }
    if let Ok(mut conn) = self.get_conn().await {
        redis::cmd("PING").query_async::<String>(&mut conn).await.is_ok()
    } else { false }
}
```
Health entirely depends on `ConnectionManager`'s configured request timeout. If
that's unset or large, a network partition leaves the health check blocked
indefinitely, making the adapter appear "pending" rather than "unhealthy". Wrap in
`tokio::time::timeout(self.config.request_timeout, …)`.

### Low

#### `RedisStreamDedup::new` accepts unbounded capacity
**File:** `src/adapter/net/../redis_dedup.rs:103-109`
`capacity.max(1)` has no upper clamp. A misconfigured `usize::MAX` pre-allocates
`VecDeque`/`HashSet` and OOMs on construction. Cheap to clamp.

#### `net_num_shards` returns 0 on null handle
**File:** `src/ffi/mod.rs:1069-1078`
Returns `0` for null handle — also a valid (if unusual) shard count. Inconsistent
with sibling FFI functions that return negative `NetError` codes; C callers can't
distinguish null handle from a 0-shard bus. Return `NetError::InvalidHandle.into()`.

### Claims investigated and rejected
Listed so future audits don't re-litigate them.

- **Off-by-one null-terminator in `net_poll`** (`ffi/mod.rs:806/817`) — the bound
  check `response_json.len() + 1 > buffer_len` is correct: when `len + 1 ==
  buffer_len`, the write at `out_buffer.add(len)` lands at the last valid byte.
- **`OnceCell` caches stream-init errors forever** (`adapter/jetstream.rs:131`) —
  the docstring at line 129-130 and `OnceCell::get_or_try_init` semantics confirm
  errors leave the cell uninitialized; subsequent calls retry.
- **`shift_bitmap_up` UB at `bit_shift == 64`** (`adapter/net/crypto.rs:495`) —
  guarded by the `if bit_shift == 0` branch at line 479; the right-shift is always
  in `[1, 63]`.
- **`session_prefix_from_id` XOR collisions** (`adapter/net/crypto.rs:511-515`) —
  irrelevant to security: keys are per-session-direction, so nonce uniqueness only
  needs `(key, nonce)` uniqueness within one key.
- **JetStream batch ordering across concurrent `on_batch`** (`jetstream.rs:262-277`)
  — the per-shard FIFO contract is enforced upstream by the shard manager
  serializing per-shard dispatch; this layer only sees one batch at a time per shard.

---

## Files audited with no bugs found

**Cortex:** `envelope.rs`, `meta.rs`, `error.rs`, `config.rs`, `mod.rs`,
`watermark.rs`, `memories/fold.rs`, `tasks/fold.rs`, `memories/state.rs`,
`tasks/state.rs`, `*/types.rs`, `*/dispatch.rs`, `memories/query.rs`,
`tasks/query.rs`, `memories/filter.rs`, `tasks/filter.rs`, `memories/watch.rs`,
`tasks/watch.rs` (well-defended; explicit prior-bug pins #134, #141, #142, #144, #148,
CR-20).

**Other:** `behavior/broadcast.rs` (only a `SUBPROTOCOL_CAPABILITY_ANN` constant —
real broadcast logic lives on `MeshNode`), `consumer/mod.rs` (re-export shim),
`noop.rs` (counter-only test adapter).

---

## Third Pass — bus / event / config / adapter / consumer / shard / ffi (2026-05-02)

Six parallel auditors over `bus.rs` (~120K), `event.rs` / `config.rs` /
`error.rs` / `timestamp.rs`, the full `adapter/` tree, `consumer/`, `shard/`,
and `ffi/`. New findings below; duplicates of items already in the second pass
(`activate_shard` double-count, JetStream `shard_id as u16` truncation,
`RedisStreamDedup::new` unbounded capacity, `net_num_shards` null-handle
ambiguity) are not re-listed.

### High

#### Activation-failure abort drops drain-worker scratch buffer
**File:** `src/bus.rs:420-463`
If `activate_shard()` fails after the ingest + drain workers have been spawned,
both tasks are `.abort()`-ed. Any events the drain worker has already pulled
from the ring buffer into its `scratch: Vec<InternalEvent>` are never sent to
the adapter. The comment at lines 441-446 acknowledges the scenario without
addressing the loss path. Drain the scratch synchronously on the failure path
(or post the events back to a recovery channel) before aborting.
**Severity: high (silent loss on partial activation failure).**

#### Batch worker abort drops in-memory `current_batch`
**File:** `src/bus.rs:1554-1643`
The batch worker holds an in-memory `BatchWorker.current_batch` between
flush ticks. On `.abort()` (used during failed activation, scale-down, and
shutdown timeouts), `BatchWorker::flush()` is never called, so any events
already pulled from the channel into the batch are lost. Channel-resident
events survive (the close signal triggers a final flush), but the worker's
own buffered window does not. Replace abort with a graceful `flush()` on
shutdown / activation-failure paths.
**Severity: high (silent loss of one batch worth of events per worker).**

#### Shutdown drain race past `in_flight_ingests`
**File:** `src/bus.rs:1685-1735` (with shutdown at `:1065-1083`, gate at `:1088-1094`)
The drain worker's final sweep gates on `drain_finalize_ready`, which is set
only after `in_flight_ingests` reaches zero. A producer that has already
incremented `in_flight_ingests` but stalls (lock contention, scheduler) before
reaching the shard-lock body can push *after* the in-flight counter is
observed at zero — by an unlucky drop ordering of the guard — stranding events
past the final sweep. The SeqCst pair orders the `push` w.r.t. the final read,
but does not prevent the producer's push from being delayed past the final
sweep iteration. Either widen the in-flight window to span lock acquisition,
or run the final sweep loop until two consecutive zero-event passes.
**Severity: high (silent loss on shutdown under contention).**

#### Redis MULTI/EXEC partial failures swallowed
**File:** `src/adapter/redis.rs:399-405`
```rust
let fut = pipe.query_async::<()>(&mut conn);
```
The unit-typed query discards the EXEC reply array. In Redis, individual
XADD commands inside a successful `MULTI/EXEC` can fail (e.g. wrong type,
OOM) while the transaction itself returns OK. The adapter currently treats
this as full success — the caller's `events_dispatched` counter ticks,
the events vanish. Decode as `Vec<Value>` and inspect each reply, or
switch to non-transactional pipelining and validate per-command results.
**Severity: high (silent data loss on transient backend errors).**

### Medium

#### `shutdown()` returns `Ok(())` after timeout-with-drops
**File:** `src/bus.rs:1065-1083`
When the in-flight wait times out at the configured grace deadline, the
code logs a warning and bumps `events_dropped` for the stranded count, but
still returns `Ok(())`. Callers can't distinguish clean from lossy shutdown.
Return a `ShutdownError::Lossy { dropped: u64 }` or surface a structured
field on the success path.
**Severity: medium (operator-visibility hole).**

#### `Drop` releases shutdown gates synchronously while workers hold `Arc<Self>`
**File:** `src/bus.rs:1273-1282`
`Drop::drop()` synchronously sets `shutdown=true` and `drain_finalize_ready=true`
without awaiting the workers. Worker tasks holding `Arc<Self>` may observe
the gate during partial destruction; the comment notes "events are lost"
but doesn't address the partial-Drop UB risk. Workers should be `take()`-ed
into a join set in an explicit shutdown path; `Drop` should at minimum
verify they were already joined.
**Severity: medium (correctness fragility on rare drop-without-shutdown).**

#### `poll_merger` ArcSwap leaves polls operating on stale topology
**File:** `src/bus.rs:472-475, 626, 777-778`
Concurrent `poll()` callers `.load()` the merger Arc; a topology change
(`add_shard` / `remove_shard`) `.store()`s a fresh merger. Polls already
in flight continue against the old shard list and can return incomplete
results during scaling. Pin the merger version into `ConsumeResponse` and
let the caller retry on mismatch, or block topology changes until in-flight
polls complete.
**Severity: medium (intermittent missed events during scaling).**

#### `flush()` baseline excludes events flushed via `remove_shard_internal`
**File:** `src/bus.rs:876-925`
`flush()` snapshots `events_ingested` then waits for
`events_dispatched + events_dropped >= target`. If a scale-down occurs
mid-flush, `remove_shard_internal` flushes the worker's pending batch
*directly to the adapter*, bypassing the `events_dispatched` increment at
`:1611`. The flush can return early before those redirected events are
durably persisted. Either bump the counter on the redirect path, or block
scale-downs while a flush is in flight.
**Severity: medium (premature flush success during concurrent scale-down).**

#### `dispatch_batch` final attempt collapses error reasons
**File:** `src/bus.rs:1499-1509`
The final attempt after retries returns `false` for all of: retryable
exhausted, non-retryable, and timeout — with one log line "Failed to
dispatch batch, dropping". Operators can't tell expected drops from
unexpected fatal errors in metrics. Tag the drop reason and emit
distinct counters (`drops_retry_exhausted` / `drops_fatal` / `drops_timeout`).
**Severity: medium (observability hole, not correctness).**

#### `unwrap_or_default()` on JetStream remote JSON
**File:** `src/adapter/jetstream.rs:104-118`
```rust
let raw = value.get("r").cloned().unwrap_or(serde_json::Value::Null);
let raw_bytes = Bytes::from(serde_json::to_vec(&raw).unwrap_or_default());
```
A malformed/oversized `r` field whose serialization fails silently
becomes empty bytes. The event is then "delivered" with an empty payload
instead of surfacing the corruption. Fail loudly: return
`AdapterError::Fatal("malformed event")`.
**Severity: medium (silent corruption on bad stream entries).**

#### Cold-stream poll walks `fetch_limit * 10` round-trips
**File:** `src/adapter/jetstream.rs:426-432`
```rust
let (max_seq, first_seq) = match stream.info().await {
    Ok(info) => (info.state.last_sequence, info.state.first_sequence),
    Err(_)   => (start_seq.saturating_add(span), 0),  // first_seq = 0
};
```
On `info()` failure for an empty/cold stream, `first_seq=0` causes the
loop at `:437` to walk every sequence from 1 to `max_seq` collecting
`NotFound`s. With `fetch_limit=101`, that's ~1010 RTTs per poll. Cache
the previous `info` result, or treat consecutive `NotFound`s as
end-of-stream after a small budget.
**Severity: medium (pathological latency, easy to reproduce).**

#### Redis dedup eviction race exceeds capacity
**File:** `src/adapter/redis_dedup.rs:156-174`
```rust
if self.seen.len() >= self.capacity {
    if let Some(evicted) = self.order.pop_front() {
        self.seen.remove(&evicted);
    }
}
self.seen.insert(...);
```
The check-then-evict-then-insert sequence isn't atomic; concurrent calls
can each see `len == capacity`, both evict, both insert, ending at
`len > capacity`. With `capacity == 1`, two threads racing on different
ids both see `len == 0`, skip eviction, and both insert. Evict-while-full
inside a single mutex-guarded section, or switch to an LRU implementation
that handles capacity atomically.
**Severity: medium (LRU bound violation, residual memory growth).**

#### Modulo bias in equal-weight candidate selection
**File:** `src/shard/mapper.rs:514`
```rust
let idx = (event_hash as usize) % candidates.len();
```
For `candidates.len()` not a power of two, low-bucket indices receive
slightly more events than high ones. With u64 hashes the bias is small
but non-zero and biases hot-shard selection over time. Replace with
rejection sampling (`fastrand`-style) or `((hash as u128 * len as u128) >> 64) as usize`
(Lemire's technique).
**Severity: medium (sustained skew, hard to debug from metrics).**

#### `per_shard_limit` silently capped at 10 000
**File:** `src/consumer/merge.rs:296-303`
```rust
let per_shard_limit = request.limit
    .div_ceil(shards.len()).max(1)
    .saturating_mul(over_fetch_factor)
    .min(10_000);
```
The hard cap silently truncates per-shard fetches. A consumer with a
caller-set `limit=200_000` over 10 shards expects 20 000/shard plus
over-fetch; it gets 10 000 with no warning. Either make the cap
configurable (carry it on `ConsumeRequest`) or surface a structured
`response.truncated_at_per_shard_cap = true`.
**Severity: medium (silent under-delivery on large polls).**

#### `TimestampGenerator` uses hard-coded baseline `0` for `delta_as_nanos`
**File:** `src/timestamp.rs:85`
```rust
let now = self.clock.delta_as_nanos(0, raw);
```
`quanta::Clock::new()` calibrates against an internal raw-TSC offset that
is *not* zero. Two `TimestampGenerator`s instantiated at different times
produce timestamps that differ by the calibration drift, breaking the
documented monotonicity invariant *across* generators. Use
`self.clock.start_raw()` (or whatever quanta exposes) as the baseline,
or store the baseline at generator construction.
**Severity: medium (cross-generator timestamp ordering corrupted).**

### Low

#### `in_flight_ingests` is `AtomicU32` with no saturating semantics
**File:** `src/bus.rs:650-652`
A 4-billion-in-flight wrap is not realistic in production, but the
counter participates in the shutdown protocol and the wrap would invert
the protocol's ordering check. Either widen to `AtomicU64` or use
`fetch_add`/`fetch_sub` with a `debug_assert` on the returned value.

#### `drain_finalize_ready` Release pairs only via implicit fence
**File:** `src/bus.rs:1088-1094`
The Release store on `drain_finalize_ready` is ordered after the
`in_flight_ingests` SeqCst spin via the implicit fence carried by SeqCst
operations. Correct today, but a future change to the spin's ordering
would silently break it. Add an explicit `fence(Ordering::Release)` or
upgrade the `drain_finalize_ready` store to SeqCst.

#### `TimestampGenerator` monotonicity stalls before the documented panic
**File:** `src/timestamp.rs:93-96`
Once `last == u64::MAX-1`, calls with `now < MAX` return `MAX` (via
`.max()` on the result), and only the call after that panics. The
monotonicity contract briefly stalls before failure. Panic preemptively
when `last == u64::MAX-1` and the input is non-strictly-greater.

#### `cpus.saturating_mul(2)` caps `max_shards: u16` at 65 535
**File:** `src/config.rs:744`
On hosts with >32 767 CPUs, `high_throughput` silently caps at 65 535
instead of the documented 2× CPU count. Edge-case-only; either widen
`max_shards` to `u32` or cap deliberately and document.

#### `Cursor::encode` returns empty cursor on serialization failure
**File:** `src/consumer/merge.rs:100-103`
```rust
let json = serde_json::to_string(&self.positions).unwrap_or_default();
```
Should be unreachable for the current `positions` schema (`HashMap<u16, Arc<str>>`),
but the silent fallback to an empty cursor would restart the consumer
from the beginning on the next poll. Propagate the error.

#### `has_more=true` from a stalled adapter is silently suppressed
**File:** `src/consumer/merge.rs:568-574`
The merger detects "adapter reports `has_more` with no progress" and
suppresses `has_more` to avoid caller infinite loops. Correctness is
preserved, but the underlying adapter bug is invisible to the caller —
they see "no more events" and exit. Surface a `stalled_shards: Vec<u16>`
on the response so callers/operators can alert.

#### `velocity_samples` `VecDeque` bounded only by time, not count
**File:** `src/shard/batch.rs:29, 62-72`
At 100 k events/s with a 100 ms `velocity_window`, the deque grows to
~10 k entries; `calculate_velocity` is then O(n) per call. Cap by sample
count as well, or sample at fixed `tick_granularity`.

#### `add_events(vec![])` flushes as a side effect
**File:** `src/shard/batch.rs:241-252`
`add_events` with an empty vector calls `check_timeout`, which can
trigger a flush of the open batch. A caller treating `add_events([])`
as a no-op is surprised. Either skip `check_timeout` on empty input
or rename / document the side-effect.

### Top 3 from this pass worth fixing first
1. `bus.rs` activation-failure scratch loss + batch-worker abort flush
   (`:420-463`, `:1554-1643`) — paired silent-loss bugs on every
   error/shutdown path.
2. `redis.rs:399-405` MULTI/EXEC partial-failure swallow — silent data
   loss on transient backend errors.
3. `jetstream.rs:426-432` cold-stream poll walking 1000+ RTTs — easy
   reproducer, observable as "JetStream is slow" in production.
